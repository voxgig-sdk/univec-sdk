---
theme: none
title: "UniVec API"
fonts:
  provider: none
mdc: false
colorSchema: dark
canvasWidth: 1120
aspectRatio: 16/9
transition: none
---

# UniVec API

UniVec embedding interoperability API.

This is an unofficial SDK for the UniVec public API, generated by Voxgig. It is not affiliated with, endorsed by, or sponsored by the upstream API provider.

---

# What this SDK gives you

- 4 entities across 8 HTTP routes
- 23 language SDKs and 3 companion tools
- 19 optional features built in, all off until you enable them

An entity groups related operations, and each operation may cover several routes. The SDK exposes both using the conventions of your language, so you write `client` calls rather than HTTP requests.

---

# API capabilities

- Convert: `create`
- Embed: `create`
- EphemeralKey: `create`
- Model: `list`

---

# Authentication

The API uses `http` authentication. Pass the credential when you construct the client, and read it from the environment rather than from source.

---

# Features come with the SDK

Retries, caching, paging, logging and the rest are compiled in and switched off. You turn on the ones your application needs in the client configuration, and pay for nothing you leave alone.

---

# Built-in features 1 / 3

- Structured audit trail of operations
- Response caching for safe read requests
- Client identity and per-request correlation headers
- Cost tracking and spend budget for API calls
- Request/response capture ring buffer for debugging
- Idempotency keys for safe retries of mutating operations
- Structured request and response logging
- Statistics capture: per-operation counters and latency

---

# Built-in features 2 / 3

- Network behaviour simulation for offline testing (latency, failures, outages)
- Pagination signals for list operations
- Outbound HTTP(S) proxy routing
- Client-side rate limiting via a token bucket
- Client-side role/permission enforcement
- Automatic retry of transient failures with exponential backoff
- Secret access: resolve the API credential through a provider chain, and exchange a refresh token for short-lived access tokens
- Incremental streaming of list results via async iteration

---

# Built-in features 3 / 3

- Distributed tracing spans with W3C trace-context propagation
- In-memory mock transport for testing without a live server
- Per-request timeout with transport abort

---

# SDKs and tools 1 / 6

- C: build from `c/`
- Clojure: build from `clojure/`
- C++: build from `cpp/`
- C#: build from `csharp/`
- Dart: build from `dart/`

---

# SDKs and tools 2 / 6

- Elixir: build from `elixir/`
- Golang: build from `go/`
- Go CLI: build from `go-cli/`
- Go MCP server: build from `go-mcp/`
- Haskell: build from `haskell/`

---

# SDKs and tools 3 / 6

- Java: build from `java/`
- JavaScript: build from `js/`
- Kotlin: build from `kotlin/`
- Lean: build from `lean/`
- Lua: build from `lua/`

---

# SDKs and tools 4 / 6

- OCaml: build from `ocaml/`
- Perl: build from `perl/`
- PHP: build from `php/`
- Python: build from `py/`
- Python Data: build from `py-data/`

---

# SDKs and tools 5 / 6

- Ruby: build from `rb/`
- Rust: build from `rust/`
- Scala: build from `scala/`
- Swift: build from `swift/`
- TypeScript: build from `ts/`

---

# SDKs and tools 6 / 6

- Zig: build from `zig/`

---

# Tutorial: your first call

Five steps, in TypeScript. Every other language in this repository follows the same shape.

1. Install the SDK.
2. Construct a client.
3. Call an operation.
4. Handle the failure.
5. Turn on a feature.

---

# Step 1: install

This target is not published yet. Build it from `ts/` in the repository.

---

# Step 2: construct a client

```ts
import { UnivecSDK } from '@voxgig-sdk/univec'

const client = new UnivecSDK({ apikey: process.env.UNIVEC_APIKEY })
```

The credential is read from the environment, so nothing secret reaches your source tree.

---

# Step 3: call an operation

```ts
const result = await client.Convert().create({
  // the input this operation requires
})
```

That reaches `POST /v1/convert`. The reference page for Convert lists every field it expects.

---

# Step 4: handle the failure

```ts
try {
  const result = await client.Convert().create({ /* input */ })
} catch (err) {
  // authentication, validation, or the API itself
}
```

Operations fail loudly rather than returning an empty result, so an error is never mistaken for no data.

---

# Step 5: turn on a feature

```json
{ "retry": { "active": true } }
```

Pass that in the client configuration and automatic retry of transient failures with exponential backoff is live. The feature pages document every option and its default.

---

# This SDK is generated

Nothing here was written by hand. An API definition goes in, and every language, test suite and page of documentation comes out.

So the useful question is not how to patch it. It is what to regenerate, and what survives when you do.

---

# Regenerate it

```sh
npm run generate
```

Update the API definition in `.sdk/def/` and regenerate. New routes, new fields and new entities reach every target at once.

---

# Add a language

```sh
voxgig-sdkgen target add <language>
```

This repository already builds 23 of them. A target added today is generated from the same model as the ones already here, so it arrives complete rather than as a stub.

---

# Add a feature

```sh
voxgig-sdkgen feature add <feature>
```

A feature is behaviour around the call, not a change to it: retries, caching, audit trails, secret resolution. It lands in every target that supports it.

---

# What survives a regeneration

- `.sdk/model/project.aon` is yours. Settings belong there.
- Everything generated is overwritten, every time.

An edit to a generated file works until the next regeneration and then disappears without warning, which is the one failure mode worth knowing in advance.

---

# Where to go next

- Read the API reference for the operation you need.
- Pick a language and follow its installation page.
- Review the feature pages before enabling anything in production.
