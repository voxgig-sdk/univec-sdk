# Univec SDK

UniVec API client, generated from the OpenAPI spec.

UniVec embedding interoperability API.

Learn more about UniVec API at [univec.ai](https://univec.ai).

This is an unofficial SDK for the UniVec public API, generated by Voxgig with [`@voxgig/sdkgen`](https://github.com/voxgig/sdkgen). It is not affiliated with, endorsed by, or sponsored by the upstream API provider.

Learn more about Voxgig SDKs at [voxgig.com/sdk](https://voxgig.com/sdk/).

> TypeScript, Python, PHP, Golang, Ruby, Lua, C, Clojure, C++, C#, Dart, Elixir, Java, JavaScript, Kotlin, Lean, OCaml, Perl, Python Data, Rust, Scala, Swift, Zig SDKs, a CLI with an interactive REPL, and an MCP server for AI agents — all generated from one OpenAPI spec by [@voxgig/sdkgen](https://github.com/voxgig/sdkgen).

> **Features:** `audit`, `cache`, `clienttrack`, `cost`, `debug`, `idempotency`, `log`, `metrics`, `netsim`, `paging`, `proxy`, `ratelimit`, `rbac`, `retry`, `streaming`, `telemetry`, `test`, `timeout` — opt-in,
> inactive until switched on, and configured per client. See the Features
> section of any SDK README below for what each one does.

## Entities, not endpoints

This SDK exposes the API as a small set of **semantic entities** — Convert, Embed, EphemeralKey and Model — that you
call directly, instead of assembling URL paths and query strings. Entities are
**Capitalised** to mark them as the primary surface, each with the operations they
support (`list`, `create`):

```ts
const client = new UnivecSDK()
const convert = await client.Convert().create({
  bridge_model: 'example',
  embeddings: [],
  source_model: 'example',
  target_model: 'example',
  texts: [],
})
```

Thinking in entities keeps the mental model small — for people and AI agents alike —
rather than reasoning about raw HTTP routes and query parameters.

## Offline unit testing

Every SDK ships a built-in **test mode** that swaps the HTTP transport for
an in-memory mock, so your unit tests run fully offline — no server, no
network, and no credentials:

### TypeScript

```ts
// The offline mock starts EMPTY — seed it with the records the test needs.
// Shape: { entity: { <entity-name>: { <id>: <record> } } }
const client = UnivecSDK.test({
  entity: {
    model: {
      test01: { id: 'test01' },
    },
  },
})
const models = await client.Model().list()
// models is an array of Model entities, populated with mock data
// — call models[0].data() for the record itself
console.log(models)
```

### Python

```python
client = UnivecSDK.test()
models = client.Model().list()
print(models)
```

### PHP

```php
// Seed fixture data so offline calls resolve without a live server.
$client = UnivecSDK::test([
    "entity" => ["model" => ["test01" => []]],
]);
$models = $client->Model()->list();
```

### Golang

```go
client := sdk.Test()
result, err := client.Model(nil).List(
    nil, nil,
)
```

### Ruby

```ruby
# Seed fixture data so offline calls resolve without a live server.
client = UnivecSDK.test({
  "entity" => { "model" => { "test01" => {} } },
})
models = client.Model.list()
```

### Lua

```lua
local client = sdk.test()
local results, err = client:Model():list()
```

### C

```c
#include "core/api.h"

UnivecSDK* client = test_sdk(NULL, NULL);
PNError* err = NULL;
Entity* model = univec_model(client, NULL);
voxgig_value* models = model->vt->list(model, NULL, NULL, &err);
printf("%s\n", voxgig_to_json(models));
```

### Clojure

```clojure
(require '[sdk.api :as api]
         '[sdk.entity.model :as e-model]
         '[voxgig.struct :as vs])

(def client (api/test-sdk nil nil))
(def models (e-model/list (api/model client nil) nil nil))
(println models)
```

### C++

```cpp
auto client = UnivecSDK::testSDK();
Value models = client->model()->list(Value::undef(), Value::undef());
std::cout << Struct::jsonify(models) << std::endl;
```

### C#

```csharp
var client = UnivecSDK.TestSDK(null, null);
var modelList = client.Model().List(null);
Console.WriteLine(modelList);
```

### Dart

```dart
import 'package:univec_sdk/UnivecSDK.dart';

Future<void> main() async {
  final client = UnivecSDK.test();
  final models = await client.Model().list();
  print(models);
}
```

### Elixir

```elixir
alias Univec.Helpers, as: H

sdk = Univec.test()
model = Univec.model(sdk)
records = Univec.Entity.Model.list(model, H.deep(%{}))
IO.inspect(records)
```

### Java

```java
UnivecSDK client = UnivecSDK.testSDK(null, null);
Object modelList = client.model(null).list(null, null);
System.out.println(modelList);
```

### JavaScript

```js
const client = UnivecSDK.test()
const models = await client.Model().list()
// models is an array of entities, populated with mock data
// — call models[0].data() for the record itself
console.log(models)
```

### Kotlin

```kotlin
val client = UnivecSDK.testSDK(null, null)
val modelList = client.model(null).list(null, null)
println(modelList)
```

### Lean

```bash
cd lean
lake build
lake exe primary        # shared corpus: request-shaping utilities
lake exe feature        # the feature catalog (retry, cache, rbac, netsim, …)
lake exe structcorpus   # shared corpus: the vendored struct model
lake exe runner         # entity behaviour (offline; add SDK_TEST_BASE for live)
```


### OCaml

```ocaml
let () =
  let client = Sdk_client.test () in
  let results = (Sdk_client.model client Noval).e_list (empty_map ()) Noval in
  List.iter (fun e -> print_endline (stringify (e.e_data_get ()))) results
```

### Perl

```perl
use lib 'perl/lib';
use UnivecSDK;

my $client = UnivecSDK->test(undef, undef);
my $models = $client->Model->list();
print scalar(@$models), " records\n";
```

### Rust

```rust
use univec_sdk::{jo, test_sdk, Value};

let client = test_sdk(Value::Noval, Value::Noval);
let models = client.model(Value::Noval).list(Value::Noval, Value::Noval).unwrap();
println!("{:?}", models);
```

### Scala

```scala
val client = UnivecSDK.testSDK(null, null)
val modelList = client.model(null).list(null, null)
println(modelList)
```

### Swift

```swift
let client = UnivecSDK.testSDK(nil, nil)
let modelList = try client.Model().list(nil, nil)
print(modelList)
```

### Zig

```zig
const std = @import("std");
const sdk = @import("sdk");
const h = sdk.h;

const client = sdk.test_sdk(h.vnull(), h.vnull());
switch (client.model(h.vnull()).list(h.vnull(), h.vnull())) {
    .ok => |models| std.debug.print("{s}\n", .{h.stringify(models)}),
    .err => |e| std.debug.print("list failed: {s}\n", .{e.msg}),
}
```

## Packages

| Language | Package | Install |
| --- | --- | --- |
| TypeScript | `@voxgig-sdk/univec` | publish pending — [install from git tag](https://github.com/voxgig-sdk/univec-sdk/releases) |
| Python | `voxgig-sdk-univec` | publish pending — [install from git tag](https://github.com/voxgig-sdk/univec-sdk/releases) |
| PHP | `voxgig-sdk/univec` | publish pending — [install from git tag](https://github.com/voxgig-sdk/univec-sdk/releases) |
| Golang | `github.com/voxgig-sdk/univec-sdk/go` | `go get github.com/voxgig-sdk/univec-sdk/go@latest` |
| Ruby | `voxgig-sdk-univec` | publish pending — [install from git tag](https://github.com/voxgig-sdk/univec-sdk/releases) |
| Lua | `voxgig-sdk-univec` | publish pending — [install from git tag](https://github.com/voxgig-sdk/univec-sdk/releases) |
| C | `voxgig-sdk-univec` | publish pending — [install from git tag](https://github.com/voxgig-sdk/univec-sdk/releases) |
| Clojure | `voxgig-sdk-univec` | publish pending — [install from git tag](https://github.com/voxgig-sdk/univec-sdk/releases) |
| C++ | `voxgig-sdk-univec` | publish pending — [install from git tag](https://github.com/voxgig-sdk/univec-sdk/releases) |
| C# | `voxgig-sdk-univec` | publish pending — [install from git tag](https://github.com/voxgig-sdk/univec-sdk/releases) |
| Dart | `voxgig-sdk-univec` | publish pending — [install from git tag](https://github.com/voxgig-sdk/univec-sdk/releases) |
| Elixir | `voxgig-sdk-univec` | publish pending — [install from git tag](https://github.com/voxgig-sdk/univec-sdk/releases) |
| Java | `voxgig-sdk-univec` | publish pending — [install from git tag](https://github.com/voxgig-sdk/univec-sdk/releases) |
| JavaScript | `@voxgig-sdk/univec-js` | publish pending — [install from git tag](https://github.com/voxgig-sdk/univec-sdk/releases) |
| Kotlin | `voxgig-sdk-univec` | publish pending — [install from git tag](https://github.com/voxgig-sdk/univec-sdk/releases) |
| Lean | `voxgig-sdk-univec` | publish pending — [install from git tag](https://github.com/voxgig-sdk/univec-sdk/releases) |
| OCaml | `voxgig-sdk-univec` | publish pending — [install from git tag](https://github.com/voxgig-sdk/univec-sdk/releases) |
| Perl | `voxgig-sdk-univec` | publish pending — [install from git tag](https://github.com/voxgig-sdk/univec-sdk/releases) |
| Python Data | `voxgig-sdk-univec-data` | publish pending — [install from git tag](https://github.com/voxgig-sdk/univec-sdk/releases) |
| Rust | `voxgig-sdk-univec` | publish pending — [install from git tag](https://github.com/voxgig-sdk/univec-sdk/releases) |
| Scala | `voxgig-sdk-univec` | publish pending — [install from git tag](https://github.com/voxgig-sdk/univec-sdk/releases) |
| Swift | `voxgig-sdk-univec` | publish pending — [install from git tag](https://github.com/voxgig-sdk/univec-sdk/releases) |
| Zig | `voxgig-sdk-univec` | publish pending — [install from git tag](https://github.com/voxgig-sdk/univec-sdk/releases) |
| Go CLI | `github.com/voxgig-sdk/univec-sdk/go-cli` | `go install github.com/voxgig-sdk/univec-sdk/go-cli/cmd/univec@latest` |
| Go MCP server | `github.com/voxgig-sdk/univec-sdk/go-mcp` | `go get github.com/voxgig-sdk/univec-sdk/go-mcp@latest` |

## Quickstart

### TypeScript

```ts
import { UnivecSDK } from '@voxgig-sdk/univec'

const client = new UnivecSDK({
  apikey: process.env.UNIVEC_APIKEY,
})

```

See the [TypeScript README](ts/README.md) for the full guide.

## Surfaces

| Surface | Path |
| --- | --- |
| **SDK** (TypeScript, Python, PHP, Golang, Ruby, Lua, C, Clojure, C++, C#, Dart, Elixir, Java, JavaScript, Kotlin, Lean, OCaml, Perl, Python Data, Rust, Scala, Swift, Zig) | `ts/` `py/` `php/` `go/` `rb/` `lua/` `c/` `clojure/` `cpp/` `csharp/` `dart/` `elixir/` `java/` `js/` `kotlin/` `lean/` `ocaml/` `perl/` `py-data/` `rust/` `scala/` `swift/` `zig/` |
| **CLI** | `go-cli/` |
| **MCP server** | `go-mcp/` |

## Use it from an AI agent (MCP)

The generated MCP server exposes every operation in this SDK as an
[MCP](https://modelcontextprotocol.io) tool that Claude, Cursor or Cline
can call directly. Build and register it:

```bash
cd go-mcp && go build -o univec-mcp .
```

Then add it to your agent's MCP config (Claude Desktop, Cursor, etc.):

```json
{
  "mcpServers": {
    "univec": {
      "command": "/abs/path/to/univec-mcp"
    }
  }
}
```

## Entities

The API exposes 4 entities:

| Entity | Description | API path |
| --- | --- | --- |
| **Convert** | The Convert entity (create). | `/v1/convert` |
| **Embed** | The Embed entity (create). | `/v1/embed` |
| **EphemeralKey** | The EphemeralKey entity (create). | `/v1/ephemeral/key` |
| **Model** | The Model entity (list). | `/v1/models` |

The operations available across these entities are **list**, **create** — see each entity's
own list above for exactly which it supports.

## Quickstart in other languages

### Python

```python
import os
from univec_sdk import UnivecSDK

client = UnivecSDK({
    "apikey": os.environ.get("UNIVEC_APIKEY"),
})

```

### PHP

```php
<?php
require_once 'univec_sdk.php';

$client = new UnivecSDK([
    "apikey" => getenv("UNIVEC_APIKEY"),
]);

```

### Golang

```go
import sdk "github.com/voxgig-sdk/univec-sdk/go"

client := sdk.NewUnivecSDK(map[string]any{
    "apikey": os.Getenv("UNIVEC_APIKEY"),
})

```

### Ruby

```ruby
require_relative "Univec_sdk"

client = UnivecSDK.new({
  "apikey" => ENV["UNIVEC_APIKEY"],
})

```

### Lua

```lua
local sdk = require("univec_sdk")

local client = sdk.new({
  apikey = os.getenv("UNIVEC_APIKEY"),
})

```

### C

```c
#include "core/api.h"

UnivecSDK* client = univec_sdk_new(cmap(1,
    "apikey", v_str(getenv("UNIVEC_APIKEY"))));
PNError* err = NULL;

```

### Clojure

```clojure
(require '[sdk.api :as api]
         '[sdk.entity.convert :as e-convert]
         '[voxgig.struct :as vs])

(def client (api/make-sdk (vs/jm "apikey" (System/getenv "UNIVEC_APIKEY"))))

```

### C++

```cpp
#include <cstdlib>
#include "core/sdk.hpp"

using namespace sdk;

const char* apikey = std::getenv("UNIVEC_APIKEY");
auto client = std::make_shared<UnivecSDK>(vmap({
    {"apikey", Value(apikey ? apikey : "")},
}));

```

### C#

```csharp
using UnivecSdk;

var client = new UnivecSDK(new Dictionary<string, object?>
{
    ["apikey"] = Environment.GetEnvironmentVariable("UNIVEC_APIKEY"),
});

```

### Dart

```dart
import 'dart:io';
import 'package:univec_sdk/UnivecSDK.dart';

Future<void> main() async {
  final client = UnivecSDK({
    'apikey': Platform.environment['UNIVEC_APIKEY'],
  });

}
```

### Elixir

```elixir
alias Univec.Helpers, as: H

sdk = Univec.new(H.deep(%{"apikey" => System.get_env("UNIVEC_APIKEY")}))

convert = Univec.convert(sdk)
```

### Java

```java
import voxgig.univecsdk.core.UnivecSDK;

Map<String, Object> options = new java.util.LinkedHashMap<>();
options.put("apikey", System.getenv("UNIVEC_APIKEY"));
UnivecSDK client = new UnivecSDK(options);

```

### JavaScript

```js
const { UnivecSDK } = require('@voxgig-sdk/univec-js')

const client = new UnivecSDK({
  apikey: process.env.UNIVEC_APIKEY,
})

```

### Kotlin

```kotlin
import voxgig.univecsdk.core.UnivecSDK

val client = UnivecSDK(mutableMapOf<String, Any?>(
    "apikey" to System.getenv("UNIVEC_APIKEY"),
))

```

### Lean

```lean
import SdkClient
open VoxgigStruct

def main : IO Unit := do
  let ctx ← mkCtx
  (do
    let sdk ← Sdk.newSdk (← emptyMap)
    IO.println s!"client ready"
  ).run ctx
```

Build it with `cd lean && lake build`.


### OCaml

```ocaml
open Voxgig_struct
open Sdk_helpers

let () =
  let client = Sdk_client.make (jo [("apikey", Str (Sys.getenv "UNIVEC_APIKEY"))]) in
```

### Perl

```perl
use lib 'perl/lib';
use UnivecSDK;

my $client = UnivecSDK->new({
    'apikey' => $ENV{'UNIVEC_APIKEY'},
});

```

### Rust

```rust
use univec_sdk::{jo, UnivecSDK, Value};

let client = UnivecSDK::new(jo(vec![
    ("apikey", Value::str(std::env::var("UNIVEC_APIKEY").unwrap_or_default())),
]));

```

### Scala

```scala
import voxgig.univecsdk.core.UnivecSDK

val options = new java.util.LinkedHashMap[String, Object]()
options.put("apikey", System.getenv("UNIVEC_APIKEY"))
val client = new UnivecSDK(options)

```

### Swift

```swift
import UnivecSdk

let options = VMap()
options.entries["apikey"] = .string(
    ProcessInfo.processInfo.environment["UNIVEC_APIKEY"] ?? "")
let client = UnivecSDK(options)

```

### Zig

```zig
const std = @import("std");
const sdk = @import("sdk");
const h = sdk.h;

const client = sdk.UnivecSDK.new(h.jo(&.{
    .{ "apikey", h.vstr(std.posix.getenv("UNIVEC_APIKEY") orelse "") },
}));

```

## Direct and prepare

For endpoints the entity model doesn't cover, use the low-level methods:

- **`direct(fetchargs)`** — build and send an HTTP request in one step.
- **`prepare(fetchargs)`** — build the request without sending it.

Both accept a map with `path`, `method`, `params`, `query`,
`headers`, and `body`. See the [How-to guides](#how-to-guides) below.

## How-to guides

### Make a direct API call

When the entity interface does not cover an endpoint, use `direct`:

**TypeScript:**
```ts
const result = await client.direct({
  path: '/api/resource/{id}',
  method: 'GET',
  params: { id: 'example' },
})
if (result instanceof Error) {
  throw result
}
console.log(result.data)
```

**Python:**
```python
result = client.direct({
    "path": "/api/resource/{id}",
    "method": "GET",
    "params": {"id": "example"},
})
```

**PHP:**
```php
$result = $client->direct([
    "path" => "/api/resource/{id}",
    "method" => "GET",
    "params" => ["id" => "example"],
]);
```

**Go:**
```go
result, err := client.Direct(map[string]any{
    "path":   "/api/resource/{id}",
    "method": "GET",
    "params": map[string]any{"id": "example"},
})
if err != nil {
    panic(err)
}
fmt.Println(result)
```

**Ruby:**
```ruby
result = client.direct({
  "path" => "/api/resource/{id}",
  "method" => "GET",
  "params" => { "id" => "example" },
})
```

**Lua:**
```lua
local result, err = client:direct({
  path = "/api/resource/{id}",
  method = "GET",
  params = { id = "example" },
})
```

**C:**
```c
PNError* err = NULL;
voxgig_value* result = sdk_direct(client, cmap(3,
    "path", v_str("/api/resource/{id}"),
    "method", v_str("GET"),
    "params", cmap(1, "id", v_str("example"))), &err);
```

**Clojure:**
```clojure
(def result
  (api/direct client
    (vs/jm "path" "/api/resource/{id}"
           "method" "GET"
           "params" (vs/jm "id" "example"))))
```

**C++:**
```cpp
Value result = client->direct(vmap({
    {"path", Value("/api/resource/{id}")},
    {"method", Value("GET")},
    {"params", vmap({{"id", Value("example")}})},
}));
```

**C#:**
```csharp
var result = client.Direct(new Dictionary<string, object?>
{
    ["path"] = "/api/resource/{id}",
    ["method"] = "GET",
    ["params"] = new Dictionary<string, object?> { ["id"] = "example" },
});
```

**Dart:**
```dart
final result = await client.direct({
  'path': '/api/resource/{id}',
  'method': 'GET',
  'params': {'id': 'example'},
});
```

**Elixir:**
```elixir
result = Univec.direct(sdk, Univec.Helpers.deep(%{
  "path" => "/api/resource/{id}",
  "method" => "GET",
  "params" => %{"id" => "example"}
}))
```

**Java:**
```java
Map<String, Object> result = client.direct(Map.of(
    "path", "/api/resource/{id}",
    "method", "GET",
    "params", Map.of("id", "example")));
```

**JavaScript:**
```js
const result = await client.direct({
  path: '/api/resource/{id}',
  method: 'GET',
  params: { id: 'example' },
})
if (result instanceof Error) {
  throw result
}
console.log(result.data)
```

**Kotlin:**
```kotlin
val result = client.direct(mutableMapOf<String, Any?>(
    "path" to "/api/resource/{id}",
    "method" to "GET",
    "params" to mapOf("id" to "example")))
```

For Lean, point the client at another server with the `base` option,
and run the entity suite against it with `SDK_TEST_BASE`:

```bash
cd lean && SDK_TEST_BASE=http://localhost:8901 lake exe runner
```

**OCaml:**
```ocaml
let result = Sdk_client.direct client (jo [
    ("path", Str "/api/resource/{id}");
    ("method", Str "GET");
    ("params", jo [("id", Str "example")]);
]) in
ignore result
```

**Perl:**
```perl
my $result = $client->direct({
    'path' => '/api/resource/{id}',
    'method' => 'GET',
    'params' => { 'id' => 'example' },
});
```

**Rust:**
```rust
let result = client.direct(jo(vec![
    ("path", Value::str("/api/resource/{id}")),
    ("method", Value::str("GET")),
    ("params", jo(vec![("id", Value::str("example"))])),
]));
```

**Scala:**
```scala
val result = client.direct(java.util.Map.of(
    "path", "/api/resource/{id}",
    "method", "GET",
    "params", java.util.Map.of("id", "example")))
```

**Swift:**
```swift
let result = client.direct(VMap([
    ("path", .string("/api/resource/{id}")),
    ("method", .string("GET")),
    ("params", .map([("id", .string("example"))])),
]))
```

**Zig:**
```zig
const result = client.direct(h.jo(&.{
    .{ "path", h.vstr("/api/resource/{id}") },
    .{ "method", h.vstr("GET") },
    .{ "params", h.jo(&.{.{ "id", h.vstr("example") }}) },
}));
```

## Advanced

> Everyday use only needs the sections above. This explains the internals
> behind every call — relevant when writing custom features.

Every SDK call runs the same five-stage pipeline:

1. **Point** — resolve the API endpoint from the operation definition.
2. **Spec** — build the HTTP specification (URL, method, headers, body).
3. **Request** — send the HTTP request.
4. **Response** — receive and parse the response.
5. **Result** — extract the result data for the caller.

A feature hook fires at each stage (e.g. `PrePoint`, `PreSpec`,
`PreRequest`), so features can inspect or modify the pipeline without
forking the SDK.

### Features

| Feature | Purpose |
| --- | --- |
| **AuditFeature** | Structured audit trail of operations |
| **CacheFeature** | Response caching for safe read requests |
| **ClienttrackFeature** | Client identity and per-request correlation headers |
| **CostFeature** | Cost tracking and spend budget for API calls |
| **DebugFeature** | Request/response capture ring buffer for debugging |
| **IdempotencyFeature** | Idempotency keys for safe retries of mutating operations |
| **LogFeature** | Structured request and response logging |
| **MetricsFeature** | Statistics capture: per-operation counters and latency |
| **NetsimFeature** | Network behaviour simulation for offline testing (latency, failures, outages) |
| **PagingFeature** | Pagination signals for list operations |
| **ProxyFeature** | Outbound HTTP(S) proxy routing |
| **RatelimitFeature** | Client-side rate limiting via a token bucket |
| **RbacFeature** | Client-side role/permission enforcement |
| **RetryFeature** | Automatic retry of transient failures with exponential backoff |
| **StreamingFeature** | Incremental streaming of list results via async iteration |
| **TelemetryFeature** | Distributed tracing spans with W3C trace-context propagation |
| **TestFeature** | In-memory mock transport for testing without a live server |
| **TimeoutFeature** | Per-request timeout with transport abort |

Pass custom features via the `extend` option at construction time.

## Customizing this SDK

This repository contains its own generator (`.sdk/`), so the SDK is
customizable without forking any upstream tool:

- **The model** (`.sdk/model/`) declares everything this project owns:
  package names, versions, active features, per-target settings. It is
  written in [aontu](https://github.com/aontu-lang/aontu), a JSON-based
  specification language designed for building ontologies: easy to edit
  by hand, and files unify rather than override, so small declarations
  compose into one model. Regeneration re-reads it every time.
- **Templates** (`.sdk/tm/`) and **components** (`.sdk/src/cmp/`) are
  the two layers of generation, copied into this repo: templates are the
  literal per-language source, components generate the API-shaped parts.
- **Regeneration merges.** By default, newly generated content is
  three-way merged into existing files, so generator updates and local
  edits usually converge without manual conflict handling. A project can
  opt for plain overwrite instead.
- **Custom features and entire custom targets** arrive through sdkgen
  packages (`voxgig-sdkgen package add`), on the same rails as the
  bundled languages, and `voxgig-sdkgen doctor` reports any drift from
  what a resync would write.

How-to: [customize and propagate templates](https://github.com/voxgig/sdkgen/blob/main/docs/how-to/customize-and-propagate-templates.md).
The full story: [voxgig.com/sdk/custom](https://voxgig.com/sdk/custom).

## Per-language documentation

- [TypeScript](ts/README.md)
- [Python](py/README.md)
- [PHP](php/README.md)
- [Golang](go/README.md)
- [Ruby](rb/README.md)
- [Lua](lua/README.md)
- [C](c/README.md)
- [Clojure](clojure/README.md)
- [C++](cpp/README.md)
- [C#](csharp/README.md)
- [Dart](dart/README.md)
- [Elixir](elixir/README.md)
- [Java](java/README.md)
- [JavaScript](js/README.md)
- [Kotlin](kotlin/README.md)
- [Lean](lean/README.md)
- [OCaml](ocaml/README.md)
- [Perl](perl/README.md)
- [Python Data](py-data/README.md)
- [Rust](rust/README.md)
- [Scala](scala/README.md)
- [Swift](swift/README.md)
- [Zig](zig/README.md)

## Upstream API

This SDK is generated from the upstream OpenAPI specification. It is an
unofficial client and is not affiliated with the API provider.

The OpenAPI spec(s) this SDK was generated from are kept in the
[`.sdk/def/`](.sdk/def/) folder.

- Upstream API: [https://univec.ai](https://univec.ai)

## Security

Please report security issues to security@voxgig.com. See [SECURITY.md](SECURITY.md).
Do not open public issues for suspected vulnerabilities.

---

Generated from the UniVec API OpenAPI spec by [@voxgig/sdkgen](https://github.com/voxgig/sdkgen).
